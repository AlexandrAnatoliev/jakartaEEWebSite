# compile all project Servlet.java files  to Servlet.class
#!/bin/bash
echo    # separate the output with an empty line
echo "start recompilation"
./compile BlogServlet
./compile DevopsServlet
./compile HomeServlet
./compile ProjectsServlet
echo "end recompilation"
